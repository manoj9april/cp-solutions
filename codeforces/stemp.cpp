#include<bits/stdc++.h>

using namespace std;

#define ll long long

#define rep(idx, from, to, inc) for (int idx=from ; i<=to ; idx+=inc)
#define loop(i,n) rep(i,0,n-1,1)

int const lmt = 1e5+7;

int main(){
    
}